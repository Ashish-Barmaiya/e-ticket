<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Venues</title>
</head>
<body>
    <h2>Your Venues</h2>
    <hr>
    <% if (!venue) { %>
        <p>You have not added any venue yet. Click below to add a venue.</p>
    <% } else { %> 
        <h4>Name: <%= venueName %></h4>
        <p>Address: <%= venueAddress %></p>
        <p>Venue Type: <%= venueType %></p>
        <p>Seating Capacity: <%= seatingCapacity %></p>
        <p>Seating Categories: <%= seatingCategories %></p>
    <% } %>
    <hr>
    <br>
    <a href="/host/<%= host.id %>/dashboard/venue/add-venue">Add Venue</a>
</body>
</html>